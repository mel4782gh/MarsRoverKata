pipeline {
    agent { any { docker 'maven:3.8.4-openjdk-11-slim' }
          }
    stages {
        stage('build') {
            steps {
                def MAVEN_HOME = tool name: 'Apache Maven 3.8.4', type: 'maven'
                    sh "${MAVEN_HOME}/mvn --version"
        }
    }
}